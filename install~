#! /bin/bash
set -e

if [ -d ~/.submit50 ]
then sudo rm -R ~/.submit50
fi
mkdir ~/.submit50
cd ~/.submit50
git clone https://github.com/qiyancos/submit50.git
sudo chmod -R 777 ~/.submit50
~/.submit50/submit50/install
